
[tool.semantic_release]
version_variable = "anime_downloader/__init__.py:__version__"
branch = "main"
changelog_file = "CHANGELOG.md"
build_command = "pip install build && python -m build"
dist_path = "dist/"
upload_to_pypi = "true"
upload_to_release = "true"
